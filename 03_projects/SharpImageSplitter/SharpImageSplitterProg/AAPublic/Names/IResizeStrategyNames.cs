using Resize = SharpImageSplitterProg.Strategies.Resize;

namespace SharpImageSplitterProg.AAPublic.Names;

public static class IResizeStrategyNames
{
    public const string FullPageStrategy = nameof(Resize.FullPageStrategy);
        
    public const string MinHalfPageStrategy = nameof(Resize.FullPageStrategy);
}
