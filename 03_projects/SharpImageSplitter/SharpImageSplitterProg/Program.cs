// using SharpImageSplitter.Helper;

namespace SharpImageSplitterProg;

public class Program
{
    public static void Main(string[] args)
    {
        // var settings = new SettingsProvider();
        // var resolutionWorker = new ResolutionWorker();
        // (var imageFolderPath, var imageFileName) = settings.GetPathTuple();
        // (var heightByWidth, var overlapPercentage) = settings.GetRatioTuple("tinder");
        //
        // //resolutionWorker.Divide(2, imageFolderPath, imageFileName);
        //
        // var imageSpliter = new ImageSpliter();
        // imageSpliter.Splitter.CreateTemp(
        //     imageFolderPath,
        //     imageFileName,
        //     heightByWidth,
        //     overlapPercentage);
    }
}