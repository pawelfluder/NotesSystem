<UserControl x:Class="WpfNotesSystem.Views.TextView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:my="clr-namespace:WpfNotesSystemProg.Converter"
        xmlns:local="clr-namespace:WpfNotesSystem"
        mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
         <ColumnDefinition></ColumnDefinition>
      </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
         <RowDefinition Height="30"></RowDefinition>
         <RowDefinition></RowDefinition>
      </Grid.RowDefinitions>

      <Border Style="{StaticResource TextViewAllRowQCol}" Grid.ZIndex="5"
         Grid.Column="0" Grid.Row="0" Grid.RowSpan="4" Grid.ColumnSpan="6"/>

      

      <!-- 0 -->
      <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
       
      <Button Content="Folder" Margin="3" Padding="5" Command="{Binding FolderCommand}"/>
         <Button Content="Content" Margin="3" Command="{Binding ContentCommand}"/>
         <Button Content="Config" Margin="3" Command="{Binding ConfigCommand}"/>
         <Button Content="Pdf" Margin="3" Command="{Binding PdfCommand}"/>
         <Button Content="GoogleDoc" Margin="3" Command="{Binding GoogledocCommand}"/>
         <Button Content="RunPrinter" Margin="3" Command="{Binding RunPrinterCommand}"/>
      </StackPanel>

      <!-- 2 -->
      <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
         <TextBox Margin="5" Text="{Binding ValueToAdd, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  MinWidth="400">
         </TextBox>
      <Button Content="Add" Margin="5" Command="{Binding AddCommand}"></Button>
      </StackPanel>

      <ScrollViewer HorizontalScrollBarVisibility="Auto"
                    MinHeight="500" MinWidth="480"
                    MaxHeight="500" MaxWidth="480"
                    Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="6">
         <ContentControl Content="{Binding HeadersDict, Converter={my:HeadersDictConverter}}" />
      </ScrollViewer>
   </Grid>
</UserControl>
